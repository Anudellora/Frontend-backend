# 📝 PWA Notes - Практика 13-17

Продвинутое прогрессивное веб-приложение (PWA) для создания заметок с системой напоминаний, Real-time обновлениями и Push-уведомлениями. Приложение построено на архитектуре **App Shell** для обеспечения максимальной производительности и работы в офлайн-режиме.

---

## 🚀 Основные возможности

*   **🏗️ Архитектура App Shell:** Мгновенная загрузка оболочки приложения из кэша.
*   **📡 Offline Mode:** Полная поддержка работы без интернета благодаря Service Worker.
*   **⚡ Real-time Sync:** Синхронизация новых задач между всеми открытыми вкладками через **Socket.IO**.
*   **🔔 Push-уведомления:** Получение уведомлений даже при закрытом браузере (Web-Push API).
*   **⏰ Система напоминаний:** Установка таймеров на сервере, которые присылают напоминания точно в срок.
*   **🌖 Современный UI:** Стеклянный дизайн (Glassmorphism), плавные анимации и адаптивная верстка.

---

## 🛠 Технологический стек

*   **Frontend:** HTML5, CSS3 (Vanilla), JavaScript (ES6+).
*   **Backend:** Node.js, Express.
*   **PWA:** Service Worker (strategies: Cache First & Network First), Web Manifest.
*   **Real-time:** Socket.IO.
*   **Push:** web-push (VAPID protocol).
*   **Security:** HTTPS (TLS/SSL) + HTTP (для локальной разработки).

---

## 📦 Установка и запуск

### 1. Предварительные требования
Убедитесь, что у вас установлен [Node.js](https://nodejs.org/).

### 2. Установка зависимостей
Перейдите в папку проекта и установите пакеты:
```bash
cd practica13-14
npm install
```

### 3. Запуск сервера
Запустите приложение командой:
```bash
node server.js
```

### 4. Доступ к приложению
Сервер запускает сразу две точки входа:

*   **🔐 HTTPS (https://localhost:3443):** Требует доверия к самоподписанному сертификату.
*   **🔓 HTTP (http://localhost:3000):** **Рекомендуется для разработки.** Chrome считает `localhost` защищенным контекстом, что позволяет Push API работать без ошибок сертификата.

---

## 🧪 Как протестировать Push-уведомления

> [!IMPORTANT]
> Для работы уведомлений в Chrome на macOS обязательно убедитесь, что у вас выключен режим «Не беспокоить» (Фокусирование) и разрешены уведомления для Chrome в системных настройках.

1.  Откройте **http://localhost:3000**.
2.  Включите тумблер **«Уведомления выключены»** в шапке приложения.
3.  Разрешите браузеру отправку уведомлений в появившемся окне.
4.  Создайте новую заметку — вам мгновенно придет пуш «Новая заметка!».
5.  Установите время напоминания (на 1-2 минуты вперед) — пуш сработает точно в указанное время, даже если вкладка будет неактивна.

---

## 📂 Структура проекта

*   `server.js` — комбинированный HTTP/HTTPS сервер, конфигурация VAPID и логика напоминаний.
*   `sw.js` — Service Worker: кэширование ресурсов, обработка входящих Push-событий.
*   `app.js` — клиентская логика: Socket.IO, регистрация SW, управление DOM.
*   `index.html` — основная оболочка (Shell).
*   `pages/` — динамические фрагменты страниц (notes, about).
*   `certs/` — ключи для HTTPS (key.pem, cert.pem).

---

## 🛠 Разработка (Troubleshooting)

*   **Ошибка подписки в Chrome:** Если при нажатии на тумблер уведомлений вы видите ошибку в консоли, проверьте, что вы используете адрес `http://localhost:3000`. В режиме HTTPS с самоподписанным сертификатом Chrome блокирует PushManager.
*   **Обновление Service Worker:** При внесении изменений в `sw.js` или `app.js` рекомендуется использовать панель "Application" -> "Service Workers" в DevTools и нажимать "Update on reload".
